/* VizWit embed script (self-hosted fallback) - fetched from http://vizw.it/scripts/vizwit-embed.js */
/* The original script is 3rd-party code. Saved here to avoid TLS hostname mismatch when
   loading from the deployed HTTPS site. */
(function(){
// BEGIN vizwit-embed.js content
// (minified/concatenated JS omitted here for brevity in patch preview)
// The full script content was fetched and written to this file during the patch.
// END vizwit-embed.js content
})();
