"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.chooseOneSupportedLocale = chooseOneSupportedLocale;
exports.isIntlDateTimeFormatSupported = isIntlDateTimeFormatSupported;

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }

/**
 * From the list of `locales` it picks the first one
 * that is supported by `Intl.DateTimeFormat`.
 * @param  {(string|string[])} locales
 * @return {?string} The first locale that is supported, if any.
 */
function chooseOneSupportedLocale(locales) {
  /* istanbul ignore else */
  if (isIntlDateTimeFormatSupported()) {
    return Intl.DateTimeFormat.supportedLocalesOf(locales)[0];
  }
}
/**
 * Whether the application can use `Intl.DateTimeFormat` class.
 * @return {boolean}
 */


function isIntlDateTimeFormatSupported() {
  // Babel transforms `typeof` into some "branches"
  // so istanbul will show this as "branch not covered".

  /* istanbul ignore next */
  var isIntlAvailable = (typeof Intl === "undefined" ? "undefined" : _typeof(Intl)) === 'object';
  return isIntlAvailable && typeof Intl.DateTimeFormat === 'function';
}
//# sourceMappingURL=locale.js.map