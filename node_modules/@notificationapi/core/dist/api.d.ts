import { API_REGION } from './interfaces';
import { Logger } from './logger';
export declare const api: (method: 'GET' | 'POST' | 'PATCH' | 'DELETE', host: string | API_REGION, resource: string, clientId: string, userId: string, hashedUserId?: string, data?: any, logger?: Logger) => Promise<any>;
export declare const generateBasicTokenForUser: (clientId: string, userId: string, hashedUserId?: string) => string;
